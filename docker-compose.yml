version: '3'
services:
    web:
        container_name: web
        build: .
        env_file:
            - ./env/web.env
        volumes:
            - .:/usr/src/app
        command: command
        ports:
            - 10000:81
        depends_on:
            - mongo
    mongo:
        container_name: mongo
        image: mongo
        ports:
            - 27017:27017
        volumes:
            - ./db/mongo:/data/db
            - ./config/mongod.conf:/etc/mongod.conf